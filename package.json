{
  "name": "@zapalniczki/logo-common",
  "version": "1.0.72",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "author": "grandalf6@gmail.com",
  "repository": "git://github.com/zapalniczki/logo-common.git",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "postinstall": "husky install",
    "build": "tsc",
    "minor": "yarn version --new-version minor",
    "patch": "yarn version --new-version patch",
    "check": "tsc --noEmit",
    "push": "git push --no-verify",
    "prepush": "echo 'pre-patch' && yarn run patch && echo 'post-patch' && echo 'pre-pull' && git pull && echo 'post-pull' && echo 'pre-merge' && git merge origin/master && echo 'post-merge' && echo 'pre-push' && git log master...origin/master && git push --no-verify && echo 'post-push'"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "typescript": "^4.7.4",
    "zod": "^3.17.3"
  }
}
