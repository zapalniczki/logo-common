{
  "name": "@logo/logo-common",
  "version": "1.0.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "tsc",
    "minor": "yarn version --new-version minor",
    "patch": "yarn version --new-version patch",
    "check": "tsc --noEmit",
    "release": "yarn run build & git add . & git commit -m \"Build\" & yarn run patch & git push"
  },
  "devDependencies": {
    "typescript": "^4.7.4",
    "zod": "^3.17.3"
  }
}
